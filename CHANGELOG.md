# CHANGELOG

> 추가형(append-only) 이력. 최종 상태는 ARCHITECTURE.md.

## (unreleased)
- chore(harness): perfect hardcore setup — initialize `.harness-engine` submodule
  (dancinlab/harness@harness-hardcore); author ARCHITECTURE.md as the real SSOT
  (overview + component map + data flow + governance/verify); rewrite CLAUDE.md to the
  harness standard (H1 + blurb + `## Structure` tree + governance + `## Harness` quick reference);
  add `docs` discipline block + `src/lib/sites.ts` lockdown core to harness.config.json;
  add a quickref pointer to TELEGRAM.md. `harness docs check` = ok · CLAUDE-MD violations = 0.
